/**
 * App Settings Type Definitions
 * These types are re-exported from the schemas package for client use
 */

// Import AppSettings from the schemas package where it's defined
export type { AppSettings } from '@promptliano/schemas'
